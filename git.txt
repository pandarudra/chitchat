// starter 


// main

git init 
git add .
git commit -m "comment"
git branch -M main
git remote add origin https://github.com/pandarudra/chitchat.git
git push -u origin main


// 2 branches

// dev  // main


// dev  : for developement
// main : main codebase for deployement

// dev

git checkout -b dev // to create a dev branch
git add .
git commit -m "comment"
git push -u origin dev


git add -A // to stage all changes (new , modified , deleted)
git add -u // to stage modified and deleted only

git switch branch_name // to change branch